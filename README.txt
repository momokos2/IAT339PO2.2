use /*=====================(name)====================*/ to separate citation, stylesheet and website styling
use /*----------------------(page name)----------------------*/ to separate code for different pages and general styling
use /* (component name) */ to separate code for different components in a page

All the components that can be re-used should be put in general styling, such as nav, footer, list, button etc. 

—————h1———: on top of each page to indicate which page is it
h2  center-alignment: page subtitles 
h3 left-align: section name
h4: description
product-list title


In-text Link  Color
Nav Button & Nav  Change
Heading Change font
Paragraph Change font